#include <stdio.h>
#include <stdlib.h>

int main() {
	int n,i,m=1,max=41000000;
	int a[11] ;
		scanf("%d",&n);
	for(i=0;i<n;i++)
	{
		scanf("%d",&a[i]);
	}
	for(i=0;i<n;i++)
	{
		if(max>=a[i])
			{
			max=a[i]; 
			m=i+1;
			}
		}
printf("%d %d",m,max);	
}
